let calc = {

    soma: (x,y) => {
        return x + y;
    },

    mult: (x,y) => {
        return x * y;
    }
};

module.exports = calc; // Exportando modulos e modulos são arquivos que contem programas para usar dentro de outros programas... Ou um pacote de código
